[pytest]
# Custom markers
markers =
    integration: marks tests as integration tests requiring external services (e.g. Ollama)

# Default: exclude integration tests so CI passes without Ollama
addopts = -m "not integration"

# Test discovery
testpaths = tests

# Asyncio mode for pytest-asyncio
asyncio_mode = auto
